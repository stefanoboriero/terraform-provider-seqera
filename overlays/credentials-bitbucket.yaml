overlay: 1.0.0
x-speakeasy-jsonpath: rfc9535
info:
  title: Bitbucket Credentials Overlay
  version: 0.0.1
actions:
- target: $.paths
  update:
    /credentials#bitbucket:
      post:
        x-speakeasy-entity-operation:

          terraform-datasource: null

          terraform-resource: BitbucketCredential#create
        x-speakeasy-name-override: CreateBitbucketCredentials
        summary: Create Bitbucket credentials
        description: Creates new Bitbucket credentials in a user context. Append `?workspaceId` to create the credentials
          in a workspace context.
        tags:
        - credentials
        operationId: CreateBitbucketCredentials
        parameters:
        - name: workspaceId
          in: query
          description: Workspace numeric identifier
          schema:
            type: integer
            format: int64
        requestBody:
          description: Bitbucket credentials create request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateBitbucketCredentialsRequest'
          required: true
        responses:
          '200':
            description: OK
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/CreateBitbucketCredentialsResponse'
          '400':
            description: Bad request
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
          '403':
            description: Operation not allowed
          '409':
            description: Conflict - resource already exists
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
        security:
        - BearerAuth: []
    /credentials/{credentialsId}#bitbucket:
      get:
        x-speakeasy-entity-operation:

          terraform-datasource: null

          terraform-resource: BitbucketCredential#read
        x-speakeasy-name-override: DescribeBitbucketCredentials
        summary: Describe Bitbucket credentials
        description: Retrieves the details of the Bitbucket credentials identified by the given `credentialsId`.
        tags:
        - credentials
        operationId: DescribeBitbucketCredentials
        parameters:
        - name: credentialsId
          in: path
          description: Credentials string identifier
          required: true
          schema:
            type: string
        - name: workspaceId
          in: query
          description: Workspace numeric identifier
          schema:
            type: integer
            format: int64
        responses:
          '200':
            description: OK
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/DescribeBitbucketCredentialsResponse'
          '400':
            description: Bad request
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
          '403':
            description: Operation not allowed
          '404':
            description: Not Found - Credential does not exist or has been deleted
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
        security:
        - BearerAuth: []
      put:
        x-speakeasy-entity-operation:

          terraform-datasource: null

          terraform-resource: BitbucketCredential#update
        x-speakeasy-name-override: UpdateBitbucketCredentials
        summary: Update Bitbucket credentials
        description: Updates the details of the Bitbucket credentials identified by the given `credentialsId`.
        tags:
        - credentials
        operationId: UpdateBitbucketCredentials
        parameters:
        - name: credentialsId
          in: path
          description: Credentials string identifier
          required: true
          schema:
            type: string
        - name: workspaceId
          in: query
          description: Workspace numeric identifier
          schema:
            type: integer
            format: int64
        requestBody:
          description: Bitbucket credentials update request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateBitbucketCredentialsRequest'
          required: true
        responses:
          '204':
            description: OK - No content
          '400':
            description: Bad request
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
          '403':
            description: Operation not allowed
        security:
        - BearerAuth: []
      delete:
        x-speakeasy-entity-operation:

          terraform-datasource: null

          terraform-resource: BitbucketCredential#delete
        x-speakeasy-name-override: DeleteBitbucketCredentials
        summary: Delete Bitbucket credentials
        description: Deletes the Bitbucket credentials identified by the given `credentialsId`.
        tags:
        - credentials
        operationId: DeleteBitbucketCredentials
        parameters:
        - name: credentialsId
          in: path
          description: Credentials string identifier
          required: true
          schema:
            type: string
        - name: workspaceId
          in: query
          description: Workspace numeric identifier
          schema:
            type: integer
            format: int64
        responses:
          '204':
            description: OK - No content
          '400':
            description: Bad request
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/ErrorResponse'
          '403':
            description: Operation not allowed
        security:
        - BearerAuth: []
- target: $.components.schemas
  update:
    BitbucketCredential:
      required:
      - name
      - keys
      type: object
      properties:
        id:
          x-speakeasy-name-override: credentials_id
          type: string
          description: Unique identifier for the credential (max 22 characters)
          x-speakeasy-param-readonly: true
          x-speakeasy-param-suppress-computed-diff: true
        name:
          minLength: 2
          maxLength: 99
          type: string
          pattern: '^[a-zA-Z0-9_-]+$'
          description: Display name for the credential. Must be 2-99 characters using only letters, numbers, underscores, and hyphens. No spaces allowed.
          x-speakeasy-param-force-new: true
        provider:
          type: string
          enum:
          - bitbucket
          default: bitbucket
          description: Cloud provider type (automatically set to "bitbucket")
          x-speakeasy-name-override: providerType
          x-speakeasy-param-readonly: true
        deleted:
          type: boolean
          description: Flag indicating if the credential has been soft-deleted
          x-speakeasy-param-readonly: true
          x-speakeasy-param-computed: true
          x-speakeasy-terraform-ignore: true
        lastUsed:
          type: string
          format: date-time
          description: Timestamp when the credential was last used
          x-speakeasy-param-readonly: true
          x-speakeasy-terraform-ignore: true
        dateCreated:
          type: string
          format: date-time
          description: Timestamp when the credential was created
          x-speakeasy-param-readonly: true
          x-speakeasy-terraform-ignore: true
        lastUpdated:
          type: string
          format: date-time
          description: Timestamp when the credential was last updated
          x-speakeasy-param-readonly: true
          x-speakeasy-terraform-ignore: true
        baseUrl:
          type: string
          description: 'Repository base URL for on-premises Bitbucket server (optional). Example: https://bitbucket.org/seqeralabs'
          example: https://bitbucket.org/myorg
          x-speakeasy-name-override: base_url
          x-speakeasy-param-computed: false
        keys:
          x-speakeasy-param-readonly: true
          x-speakeasy-entity: BitbucketCredential
          x-speakeasy-entity-version: 1
          x-speakeasy-terraform-resource-name: bitbucket_credential
          x-speakeasy-entity-description: 'Manage Bitbucket credentials in Seqera platform using this resource.


            Bitbucket credentials store authentication information for accessing Bitbucket

            repositories within the Seqera Platform workflows.

            '
          type: object
          required:
          - username
          - token
          properties:
            username:
              type: string
              description: Bitbucket account username (for app passwords) or email (for API tokens). Required.
              example: myuser@example.com
              x-speakeasy-param-computed: false
            token:
              type: string
              description: Bitbucket API token (required, sensitive). App passwords are deprecated.
              x-speakeasy-param-sensitive: true
              x-speakeasy-terraform-write-only: true
              example: ATBB...
    CreateBitbucketCredentialsRequest:
      type: object
      properties:
        credentials:
          $ref: '#/components/schemas/BitbucketCredential'
    CreateBitbucketCredentialsResponse:
      type: object
      properties:
        credentialsId:
          type: string
    UpdateBitbucketCredentialsRequest:
      type: object
      properties:
        credentials:
          $ref: '#/components/schemas/BitbucketCredential'
    DescribeBitbucketCredentialsResponse:
      type: object
      properties:
        credentials:
          $ref: '#/components/schemas/BitbucketCredential'
- target: $.components.schemas.BitbucketCredential.properties.description
  remove: true
- target: $.components.schemas.BitbucketCredential.properties.category
  remove: true
- target: $.components.schemas.BitbucketCredential.properties.keys.properties.password
  remove: true
- target: $.components.schemas.BitbucketCredential.properties.keys.properties.token
  update:
    writeOnly: true
- target: $["paths"]["/credentials#bitbucket"]["post"]["parameters"][?(@.name == "workspaceId")]
  update:
    x-speakeasy-param-force-new: true
